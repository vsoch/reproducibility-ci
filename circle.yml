general:
  artifacts:
    - "analysis/output"

test:
  override:
    - python config.py
    - nosetests --where=analysis --with-doctest --verbosity=3 --nocapture

dependencies:
  pre:
    - sudo apt-get update; sudo apt-get install -y --force-yes r-base r-base-dev
